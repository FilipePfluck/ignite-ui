import { Meta, StoryObj } from '@storybook/react'

import { Checkbox, CheckboxProps } from '@pfluck-ignite-ui/react'

export default {
  title: 'Form/Checkbox',
  component: Checkbox,
} as Meta<CheckboxProps>

export const Primary: StoryObj<CheckboxProps> = {}
